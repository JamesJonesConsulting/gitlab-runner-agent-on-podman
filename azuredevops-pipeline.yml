parameters:
  - name: host
    displayName: Linux Hostname (example.com)
    type: string
    default:
      - asuspn51
      - jamacer

extends:
  template: .installer.yml    
  parameters:
    host: ${{ paramters.host }}

