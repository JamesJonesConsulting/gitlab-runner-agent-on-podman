parameters:
  - name: host
    displayName: Linux Hostname (example.com)
    type: string
    default: jamacer
    values:
      - asuspn51
      - jamacer

extends:
  template: setup.yml    
  parameters:
    host: ${{ parameters.host }}

