parameters:
  - name: host
    displayName: Linux Hostname (example.com)
    type: string
    default: jamacer
    values:
      - asuspn51
      - jamacer

extends:
  template: .installer.yml    
  parameters:
    host: ${{ parameters.host }}

